To run the server config the properties file (configserver.properties):

# Password server
password=admin
# Host
host=localhost
# Port RMI
portRmi=8888
# Port Server
portServer=8889
# Port Executor
portExecutor=8890
# jre bin folder
jreBinFolder=/usr/bin/jvm/java-6-openjdk

To run the server issue the command (to change the values again, issue the command with new values):

java -jar es.optsicom.res.server-*.jar
  
For more information:

http://code.sidelab.es/projects/optsicomres/wiki
  